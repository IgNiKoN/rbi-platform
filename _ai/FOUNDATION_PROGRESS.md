# Foundation Progress Tracker (компактный, читать вместо полного §29 в обычном цикле)

> Источник истины по **деталям** каждого пункта — `_ai/PLATFORM_TARGET_ARCHITECTURE.md`, раздел 29 (полный текст с историей закрытия). Этот файл — только компактный статус + 1 строка «что осталось», обновляется Архитектором **каждый раз**, когда меняется статус любого пункта (не раз в 5 шагов). Если этот файл расходится с §29 — §29 приоритетнее, расхождение — документационная находка, исправить оба файла напрямую тем же ответом.
>
> **Когда читать полный §29 вместо этого файла:** аудит/спор по архитектуре (архитектор `architect.rule.md` шаг 2), либо когда нужны детали закрытия конкретного пункта (даты, конкретные проверки), которых этот трекер не содержит.

**Прогресс: 13/13 пунктов закрыто целиком (пункты 1–13 ✅). Решение пользователя (2026-07-13, по вопросу архитектора): подпункт 10(в) закрыт без реального ограничения ролей — все текущие роли явно разрешены на оба бизнес-модуля (`quality`/`construction`) через `permission.service.js`, реально подключено к welcome-экрану (`app-shell.js`). Критерий готовности Foundation закрыт полностью — как по 3 подкритериям, так и по таблице пунктов целиком.**

| # | Пункт | Статус | Что осталось (если не ✅) |
|---|-------|--------|---------------------------|
| 1 | `index.js` для модулей | ✅ | — |
| 2 | `entry` в manifest + module-loader | ✅ | — |
| 3 | Стабилизация загрузки модулей | ✅ | — |
| 4 | Legacy inventory | ✅ | — |
| 5 | Перенос крупных блоков `app.js` в модули | ✅ | — |
| 6 | Standalone legacy JS decomposition | ✅ | — |
| 7 | `index.html` inventory | ✅ | — |
| 8 | Очистка `index.html` до app shell | ✅ (2026-07-13) | Блок 1 (decomposition `quality` → `#app-content`, `getContentRoot()` переключён) выполнен, headless-регресс пройден, пользователь подтвердил результат 2026-07-13. |
| 9 | App Shell (sidebar/header/toasts) | ✅ (2026-07-13) | Блок 2 (multi-select welcome-экран, company block, ребрендинг «RBI Platform») реализован, headless-регресс пройден, пользователь подтвердил визуально 2026-07-13. Header user block — готов (закрыто ранее). Полноценный постоянный sidebar — сознательно снят по уточнению модели (не мандат этого пункта, см. `current_plan.md` Блока 2).
| 10 | Auth Gate + User Context | ✅ (2026-07-13) | (а) экран входа — готово. (б) параметризация `permission.service.js` под `companyId` — готово. (в) реальное разграничение `availableModules` по роли — готово: `ALL_MODULES` (7 legacy id) → `BUSINESS_MODULE_IDS = ['quality','construction']` во всех ролях (осознанное решение пользователя «не ограничивать сейчас»), `app-shell.js` реально подключает `getAllowedModules()` к welcome-экрану (disabled-карточки для недоступных модулей, блокировка программного выбора). Организационные роли §23 — отдельный будущий блок, не входит в критерий закрытия. |
| 11 | Minimal Company Context | ✅ | — |
| 12 | Финальная очистка техдолга §16 / физическая изоляция модулей | ✅ | — (закрыт 2026-07-12: fallback-стаб `module.engineer` переведён на `window.EngineerActions.renderEngineerTab()`; `window._auditCurrentCommentId`/`app-mode-utils.js`-фасады/`tutorial.js:40` зафиксированы как постоянные архитектурные исключения, не техдолг) |
| 13 | Архитектурный аудит критерия готовности | ✅ | Закрыт 2026-07-12: `bootstrap.js:189,232`/`smart-input.utils.js:106,160` подтверждены переведёнными на `window.RBI.services.storage.getAll(...)` (Grep). Все 3 подкритерия готовности Foundation ✅. |

## Критерий готовности Foundation (кратко, полный текст — §29 конец раздела)

- `app.js` разобран до нуля бизнес-логики — ✅ выполнено (файл физически удалён).
- Весь техдолг §16 устранён (0 прямых `window.*`/`dbPut`/`dbGet`/`triggerSync`/`appSettings` вне сервисного слоя) — ✅ см. пункт 12 выше.
- Каждый platform module физически изолирован (только через `ctx`/`services`/`EventBus`) — ✅ см. пункт 12 выше (кроме сознательно принятых постоянных исключений, не техдолга).

## Последнее обновление

2026-07-16 (архитектор, подтверждение закрытия блока «Удаление осиротевшего `etalon-prompt-modal`»): §29 без изменений (13/13, Foundation закрыт целиком). Отчёт исполнителя чистый (READY_FOR_REVIEW, 0 ошибок, headless-регресс + функциональная проверка потока «Приемка Эталона»→«Снять Эталон»→`openEtalonConstructor` пройдены, регресс-навигация по 6 маршрутам без ошибок). Блок не критичный — закрывается без дополнительного ожидания. **Инициатива ROADMAP «Перенос статичной разметки `quality`+31 modal-блока+sidebar» закрыта целиком (31/31 + sidebar подтверждён).**

2026-07-15 (архитектор, план «Удаление осиротевшего `etalon-prompt-modal`»): §29 без изменений (13/13, Foundation закрыт целиком). Пользователь подтвердил критичный шаг «Sidebar-навигация, вариант A» (headless-регресс на 2 брейкпоинтах зелёный, фикс выравнивания принят). Из 2 оставшихся направлений выбран `etalon-prompt-modal`; на уточняющий вопрос архитектора о триггере/действии кнопки пользователь ответил — старую логику убрать, восстанавливать не нужно. Аудит архитектора (Grep) подтвердил: модалка полностью осиротевший UI (кнопка `btn-start-etalon` без `onclick`/`data-action`, 0 точек вызова самой модалки кроме себя), бизнес-поток «требуется эталон» уже закрыт другим путём (`tasks.module.js` → задача «Приемка Эталона» → «Снять Эталон» → `openEtalonConstructor()`, уже рабочий). Текущий план (`current_plan.md`) — обычный шаг, удаляет 21 строку `index.html` (524-544), не трогает ни одного `.js`-файла. После исполнения и подтверждённого закрытия — инициатива ROADMAP «Перенос статичной разметки `quality`+31 modal-блока+sidebar» будет закрыта целиком (31/31 + sidebar). Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, план «Decomposition `construction` в JS-рендер»): §29 без изменений (13/13, Foundation закрыт целиком). Step 41 (30/31 modal-блоков) подтверждён закрытым без новых хвостов. Из 2 открытых вопросов отчёта пользователь решил: (1) `etalon-prompt-modal` — восстановить недостающую точку вызова и перенести функционально (отдельный будущий блок, требует проектирования бизнес-логики триггера — не механический перенос, не в объёме текущего блока); (2) sidebar-навигация — уточнён объём (persistent sidebar на ПК ≥768px), но пользователь потребовал сначала разобрать статику `construction` в `index.html`, чтобы sidebar проектировался над полностью decomposed `#app-content`. Текущий план (`current_plan.md`) — перенос 3 вкладок `construction` (174 строки) в `construction.render.js`, тот же класс механического переноса, что уже 5 раз пройден для `quality`+modal-блоков. Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, план «Перенос 30 из 31 modal/overlay-блоков `#app-modals` в JS-рендер»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 4/N (`settings`, финальный для 5 features `quality`) подтверждён закрытым без хвостов — инициатива «5 features» закрыта целиком. Из 2 оставшихся направлений инициативы (sidebar-навигация vs 31 modal-блок) архитектор выбрал перенос modal-блоков — больший объём остатка, тот же механический класс риска, уже 4 раза успешно закрытый. Построчная инвентаризация (Python, подсчёт глубины `<div>`) подтвердила ровно 31 top-level блок внутри `#app-modals` в 19 owner-файлах (5 `js/shared/**`, 12 `js/modules/quality/**`, 2 `js/modules/construction/**`). Найдено 2 межмодульных примитива без владельца-фичи (`modal-overlay`/`photo-source-modal` — закреплены за `notify.utils.js`/`photo-editor.utils.js`, уже владеющими их поведением) и 1 риск тайминга (`photo-viewer-zoom.utils.js` читает DOM в top-level module-scope константе — монтаж должен физически предшествовать этой строке). `etalon-prompt-modal` (1 из 31) сознательно исключён из объёма — Grep не нашёл ни одной точки вызова в коде (осиротевший/недоделанный UI), остаётся статичным до отдельного решения архитектора/пользователя. Текущий план (`current_plan.md`) — обычный шаг, перенос 30/31 блоков. Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, план «Перенос статичной разметки `quality` в JS-рендер, Блок 4/N, финальный — `settings`»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 3/N (`reference`) подтверждён закрытым без хвостов (отчёт исполнителя чистый — 8/8 проверок, побайтовое совпадение разметки подтверждено программным diff). Текущий план (`current_plan.md`) — Блок 4/N, финальная и крупнейшая фича `quality` (`settings`, 1085 строк, границы `index.html:445-1529` подтверждены построчным подсчётом глубины `<div>`). После этого блока перенос всех 5 features `quality` в JS-рендер закрыт целиком — следующий шаг инициативы: sidebar-навигация либо 31 modal-блок `#app-modals`. Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, план «Перенос статичной разметки `quality` в JS-рендер, Блок 3/N — `reference`»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 2/N (`engineer`+`analytics`) подтверждён закрытым без хвостов (отчёт исполнителя чистый — 8/8 проверок, headless-смоук зелёный, побайтовое совпадение разметки подтверждено, `check-module-boundaries.sh` — 100% line-shift, 0 новых нарушений). Текущий план (`current_plan.md`) — Блок 3/N, фича `reference` (769 строк, единственный владеющий файл — `reference.js`, 1044 строки). По правилу размера блока — весь объём фичи переносится за один блок целиком, не дробится по 5 внутренним подвкладкам. Grep подтвердил (как и в Блоке 2/N) отсутствие top-level `bootstrap:*`-подписок и риска тайминга — единственные top-level `document.addEventListener` файла (delegated click/change) вызываются из `bindCtx(ctx)`, не на верхнем уровне модуля. Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, план «Перенос статичной разметки `quality` в JS-рендер, Блок 2/N — `engineer`+`analytics`»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 1/N (`audit`) подтверждён закрытым без хвостов (отчёт исполнителя чистый — 7/7 проверок, headless-смоук зелёный, побайтовое совпадение разметки подтверждено). Текущий план (`current_plan.md`) — Блок 2/N, фичи `engineer` (181 строка) + `analytics` (356 строк), объединены в один блок по правилу размера (сопоставимый объём/риск, без искусственного дробления). Находка при планировании: в отличие от `audit`, ни `engineer.*`, ни `analytics.*` не регистрируют top-level `bootstrap:*`-подписки (Grep подтвердил) — риска тайминга нет, монтаж разметки безопасен независимо от точки вызова (выбран тот же паттерн «верхний уровень модуля» для консистентности, не по необходимости). Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, план «Перенос статичной разметки `quality` в JS-рендер, Блок 1/N — `audit`»): §29 без изменений (13/13, Foundation закрыт целиком). Финальный блок инициативы «Разбор inline onclick/onchange» подтверждён закрытым без хвостов (отчёт исполнителя чистый — все проверки пройдены, единственные открытые пункты отчёта — необязательные рекомендации, не блокирующие: визуальный клик по 2-3 модалкам человеком и подтверждение закрытия в ROADMAP, обе не требуют действий архитектора). Пользователь выбрал из 6 предложенных направлений ROADMAP следующую инициативу — «Перенос статичной разметки `quality` (5 features, ~2565 строк) + 31 modal-блока в JS-рендер + полноценный sidebar» (ранее отмечена как крупная и рискованная, требующая отдельного явного решения — получено). Инициатива разбита на под-блоки по фиче (правило «независимо проверяемые части высокого риска»): Блок 1/N — `audit` (175 строк, наименьшая фича с наивысшей видимостью — welcome-экран первого входа). Критичная находка при планировании: `audit.render.js` регистрирует top-level слушателей `bootstrap:selectorReady`/`checklistReady`, срабатывающие раньше `AuditModule.init(ctx)` — разметку нужно монтировать синхронно на верхнем уровне модуля, не внутри `init()`, иначе список чек-листов останется пустым при первой загрузке. Не двигает счётчик §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, план «Разбор inline onclick/onchange — ФИНАЛЬНЫЙ блок, весь остаток инициативы»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 3/N (`knowledge`+`gamification`) подтверждён закрытым без хвостов (отчёт исполнителя чистый — 7/7 проверок). По прямому указанию пользователя («оставшиеся шаги текущей инициативы делаем за 1 раз») весь оставшийся объём инициативы (20 групп владения, ~104 идентификатора/162 узла, 21 файл кода) собран в один финальный блок вместо продолжения серией «Блок 4/N, 5/N...». Точным построчным Python-разбором `index.html` (не эвристикой) подтверждён реальный остаток и владение каждой группы. Сознательное исключение: `storage.js` (2 идентификатора) не переводится — файл высокого риска, трогается только отдельным явным критичным блоком. Текущий план (`current_plan.md`) не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-15 (архитектор, документационная правка + план «Разбор inline onclick/onchange, Блок 3/N — `knowledge`+`gamification`»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 2/N (`settings`) подтверждён закрытым без хвостов. Документационная находка исправлена до плана: `_ai/INDEX_HTML_HANDLERS_MAP.md` содержал устаревшие пути для owner `knowledge`/`gamification` (52 строки, `js/modules/knowledge/**`→`js/modules/quality/features/knowledge/**` и аналогично `gamification`) — исправлено напрямую, без кода. Текущий план (`current_plan.md`) — Блок 3/N ROADMAP (`knowledge`+`gamification`, объединены по правилу размера блока). Не двигает §29.

2026-07-15 (архитектор, план «Разбор inline onclick/onchange, Блок 2/N — `settings`»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 1/N (`ai`) подтверждён закрытым без хвостов (отчёт исполнителя чистый). Находка при планировании: паттерн Блока 1/N (статический строковый аргумент) не покрывает группу `settings` — требуется расширение механизма (DOM-элемент/динамическое значение/`Event`-аргумент/двойной вызов на одном узле), заложено в объём этого же блока. Текущий план (`current_plan.md`) — Блок 2/N (группа `settings`, 20 идентификаторов, 56 DOM-узлов). Не двигает счётчик §29.

2026-07-15 (архитектор, план «Разбор inline onclick/onchange, Блок 1/N — `ai`»): §29 без изменений (13/13, Foundation закрыт целиком). Критичный шаг «Codegen sw.js precache» подтверждён пользователем. Текущий план (`current_plan.md`) относится к `_ai/ROADMAP.md` — инициатива «Разбор 359 inline onclick/onchange», Блок 1/N (группа `ai`). Не двигает счётчик §29.

2026-07-15 (архитектор, план «Codegen-скрипт для `sw.js` precache-листа, часть 2», критичный шаг): §29 без изменений (13/13, Foundation закрыт целиком). Пользователь подтвердил критичный шаг «Codegen-скрипт для `sw.js` precache-листа» Блок 1 (2026-07-14). Открытый вопрос отчёта перепроверен архитектором повторным запуском скрипта — подтверждён хвост из 10 файлов (`construction`/`ai`/`gamification`/`sk` — `*.manifest.js`/`index.js`), 0 кандидатов на удаление. Текущий план (`current_plan.md`) — Блок 2/2 той же инициативы ROADMAP, не двигает §29.

2026-07-14 (архитектор, план «Реальная изоляция модулей, часть 3», все 3 группы за раз): §29 без изменений (13/13, Foundation закрыт целиком). Пользователь запросил объединить все 3 группы аудита в один план с проверкой после каждой группы. При планировании найдена и исправлена ошибка первого аудита (Grep пропускал `if (cond) X = value;` на одной строке) — `currentTemplateKey`/`currentChecklist` переклассифицированы из Группы A в Группу C (bare-запись в `session.service.js`). Текущий план (`current_plan.md`) — Группа A + Группа B (обычные шаги) + Группа C (критичный шаг, обязательна реальная Supabase-проверка + подтверждение пользователя перед закрытием). Не двигает счётчик §29 — относится к `_ai/ROADMAP.md`.

2026-07-14 (архитектор, подтверждение закрытия «часть 2» + аудит «часть 3»): §29 без изменений (13/13, Foundation закрыт целиком). Волна 3 (финал) подтверждена исполнителем — отчёт чистый (5/5 проверок, 0 ошибок, `check-module-boundaries.sh` без новых нарушений). Инициатива «Реальная изоляция модулей, часть 2» закрыта целиком в объёме `features/**`. Параллельно выполнен аудит без кода «часть 3» (физический перенос владения 13 переменных `bootstrap.js`) — см. `_ai/agent-reports/BOOTSTRAP_OWNERSHIP_AUDIT.md` и `_ai/ROADMAP.md`. Не двигает счётчик §29 — обе инициативы относятся к `_ai/ROADMAP.md`.

2026-07-14 (архитектор, план «Реальная изоляция модулей, часть 2, Волна 3, финал»): §29 без изменений (13/13, Foundation закрыт целиком). Волна 2 подтверждена исполнителем (отчёт чистый, под-объём `etalonActsArray`/`contractorArray`/`userTemplates` закрыт). Текущий план (`current_plan.md`) — Волна 3 (финал), хвост `reportsArray`/`customExpertConclusions` в 2 файлах `analytics.*`. После этого блока инициатива «часть 2» закрыта целиком в объёме `features/**`. Не двигает счётчик §29 — относится к `_ai/ROADMAP.md`.

2026-07-14 (архитектор, план «Реальная изоляция модулей, часть 2, Волна 2»): §29 без изменений (13/13, Foundation закрыт целиком). Волна 1 подтверждена исполнителем (отчёт чистый). Текущий план (`current_plan.md`) относится к `_ai/ROADMAP.md` — Волна 2 (1 файл кода, `tasks.module.js`). Не двигает счётчик §29.

2026-07-14 (архитектор, план «Реальная изоляция модулей, часть 2, Волна 1»): §29 без изменений (13/13, Foundation закрыт целиком). Текущий план (`current_plan.md`) относится к `_ai/ROADMAP.md` — инициатива «часть 2» (глобальный state `bootstrap.js`), Волна 1 (`session`-квинтет + `etalonActsArray`/`contractorArray`, 6 файлов). Не двигает счётчик §29.

2026-07-14 (архитектор, закрытие инициативы «ES Import Migration» целиком): §29 без изменений (13/13, Foundation закрыт целиком, не относится к этой записи). Критичные под-блоки `storage`/`sync`/`core` (`rbi-core.js`+`bootstrap.js`) подтверждены пользователем визуально. Инициатива ROADMAP «Нативные ES import вместо classic-script» закрыта целиком (66/66 файлов) — не двигает §29 (инициатива вне определения «завершение рефакторинга», зафиксировано пользователем 2026-07-13).

2026-07-14 (архитектор, план «ES Import Migration — критичный под-блок `sync`»): §29 без изменений (13/13, Foundation закрыт целиком). Критичный под-блок `storage` подтверждён пользователем (включая находку `PhotoManager`). Текущий план (`current_plan.md`) — критичный под-блок `sync` (8 файлов) той же инициативы ROADMAP, не двигает §29.

2026-07-13 (архитектор, план «ES Import Migration — критичный под-блок `storage`»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 6/N (`services`, 22 файла) подтверждён закрытым без хвостов. Пользователь выбрал продолжение инициативы ROADMAP — критичный под-блок `storage` (6 файлов, IndexedDB-слой), с обязательной реальной проверкой Supabase-синхронизации. Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-13 (архитектор, план «ES Import Migration, Блок 5/N — `core`»): §29 без изменений (13/13, Foundation закрыт целиком). Блок 4/N (`shared`) подтверждён закрытым с зафиксированным хвостом (`template.utils.js`/`smart-input.utils.js` остаются classic, отдельное будущее решение, не блокирует). Документационная находка исправлена архитектором до плана: `_ai/ES_IMPORT_MIGRATION_MAP.md` ошибочно считал `rbi-core.js` безрисковым — на деле это единственный файл 66-набора с guard-паттерном `window.RBI`, перевод которого в модуль раньше остальных 27 merge-паттерн-файлов сломал бы инициализацию всего `window.RBI`; исключён из объёма. Текущий план (`current_plan.md`) — Блок 5/N ROADMAP, перевод 4 из 6 файлов группы `core` (`router.js`/`views.js`/`app-shell.js`/`app.entry.js`; `rbi-core.js`/`bootstrap.js` остаются classic). Не двигает §29 — относится к `_ai/ROADMAP.md`.

2026-07-13 (исполнитель, блок §29 п.10в — закрытие Foundation 13/13): выполнено и проверено. `permission.service.js`: `ALL_MODULES` → `BUSINESS_MODULE_IDS = ['quality','construction']`, 7 записей `ROLE_MATRIX.allowedModules` + fallback `getAllowedModules()`. `app-shell.js`: новый хелпер `getRoleAllowedBusinessModuleIds()`, вызывается в `renderModuleSelection()` (disabled-карточка для недоступных модулей) и `toggleModuleSelection()` (блокировка программного выбора). Headless Chromium (Playwright, временно установлен/удалён): 0 console.error/pageerror/requestfailed/HTTP≥400, 7/14 `[app.entry] ✅ module.*` (загрузка/reload); все 7 ролей → `['quality','construction']`; welcome-экран — 2 активные карточки, 0 disabled бизнес-карточек, 8 disabled placeholder — визуальная регрессия отсутствует. `check-module-boundaries.sh` не требовался (файлы вне `js/modules/**`). §29 п.10 закрыт целиком — **13/13**.

2026-07-13 (архитектор, план §29 п.10в — закрытие Foundation 13/13): пользователь выбрал (из 3 предложенных вариантов) не ограничивать роли сейчас — все роли явно разрешены на оба бизнес-модуля. Находка при планировании: `getAllowedModules()`/`getUserContext().availableModules` — рабочий код без потребителей в UI; `allowedModules` у всех ролей — устаревший `ALL_MODULES` (7 legacy module-id вместо 2 реальных бизнес-модулей после инициативы «Декларативная классификация role»). Текущий план (`current_plan.md`) — обычный шаг: `permission.service.js` (`ALL_MODULES`→`BUSINESS_MODULE_IDS`), `app-shell.js` (`renderModuleSelection`/`toggleModuleSelection` реально спрашивают `getAllowedModules()`). Двигает пункт 10 к закрытию целиком (12/13 → 13/13 после исполнения и проверки).

2026-07-13 (архитектор, план §29 п.10б — параметризация `permission.service.js` под `companyId`): пользователь выбрал этот вариант из 3 предложенных для продолжения работы над пунктом 10 (после закрытия пункта 9). Документационная находка закрыта до плана: `_ai/SERVICES_API.md` секция `permissions` дополнена 10 недокументированными существующими методами (`getDataScope`/`getAllowedModules`/`getContract`/`filterByDataScope`/`getAllRoles`/`hasNoOwnObjects`/`getAssignedProjects`/`getAssignedContractor`/`applyUIConstraints`/`can`). Текущий план (`current_plan.md`) — обычный шаг, оборачивает `ROLE_MATRIX` в `COMPANY_ROLE_MATRICES` под ключ `rbi`, добавляет резолвер `_getRoleMatrix(companyId)`, параметризует 6 методов опциональным `companyId` с полной обратной совместимостью (fallback на `rbi`). Не двигает счётчик §29 (пункт 10 остаётся 🟡 — подпункт (в) не начат, требует бизнес-решения).

2026-07-13 (архитектор, план «Декларативная классификация role»): Блок 2 App Shell (§29 п.9) подтверждён пользователем визуально после headless-регресса — **пункт 9 закрыт** (11/13 → пункты 1–9, 11–13 ✅). Открытый вопрос отчёта Блока 2 (`check-module-boundaries.sh quality` line-shift на 20 строк известных baseline-исключений) закрыт по прямому прецеденту блоков 2/6 и 4/6 ROADMAP — не редактировать `known-boundary-debt.txt`, реальных новых нарушений 0. Текущий план (декларативная классификация `role: module/feature-of/service` вместо `type: business/platform`) не двигает §29 — по `_ai/ROADMAP.md`, зафиксирован как следующий блок сразу после App Shell Блок 2.

2026-07-13 (архитектор, план App Shell Блок 2, критичный шаг): Блок 1 App Shell (§29 п.8, decomposition `index.html` для `quality` → `#app-content`) подтверждён пользователем как выполненный без регрессий — **пункт 8 закрыт** (10/13 → пункты 1–8, 11–13 ✅). Следующий блок — Блок 2 App Shell целиком (§22/§29 п.9): sidebar, company block, ребрендинг «RBI Platform», welcome-экран с карточками всех модулей (поглощает §37.1). Критичный шаг — план ждёт выполнения + отдельного подтверждения пользователя. Текущий план двигает **пункт 9**.

2026-07-13 (архитектор, план App Shell Блок 1, повторный запуск §29 п.8): Блок 6/6 (`construction`, ROADMAP «Миграция `window.*`») подтверждён закрытым без хвостов — инициатива ROADMAP завершена целиком (6/6). Следующий незакрытый критичный пункт §29 — п.8 (Блок 1 App Shell, decomposition `index.html` для `quality`), предложен пользователю явно (не пропущен в пользу лёгких некритичных пунктов), подход уже был согласован ранее (2026-07-12) и сейчас запускается повторно тем же согласованным объёмом. Текущий план двигает **пункт 8**.

2026-07-13 (архитектор, план Блока 6/6, финальный): §29 без изменений (10/13, Foundation закрыт по 3 подкритериям). Блок 5/6 (`settings`) подтверждён закрытым без хвостов (открытый вопрос отчёта — арифметическая опечатка плана «31 vs 30», не ошибка реализации). Текущий план (Блок 6/6, модуль `construction`, финальный блок инициативы) не двигает §29 — относится к инициативе `_ai/ROADMAP.md` «Миграция `window.*`-нарушений публичной границы модуля».

2026-07-13 (архитектор, документационная правка + план Блока 5/6): §29 без изменений (10/13, Foundation закрыт по 3 подкритериям). Закрыт открытый вопрос отчёта Блока 4/6 (`sk`) — `_ai/scripts/known-boundary-debt.txt` пересчитан (903→773 строк, секция `sk` line-shift, не новый техдолг), `check-module-boundaries.sh` → `OK: 0 new violations`, Блок 4/6 закрыт полностью. Текущий план (Блок 5/6, модуль `settings`) не двигает §29 — относится к инициативе `_ai/ROADMAP.md` «Миграция `window.*`-нарушений публичной границы модуля».

2026-07-13 (архитектор, план Блока 4/6): §29 без изменений (10/13, Foundation закрыт по 3 подкритериям). Блок 3/6 (`knowledge`) подтверждён закрытым полностью (исполнитель отчитался, открытых хвостов нет). Текущий план (Блок 4/6, модуль `sk`) не двигает §29 — относится к инициативе `_ai/ROADMAP.md` «Миграция `window.*`-нарушений публичной границы модуля».

2026-07-13 (архитектор, документационная правка + план Блока 3/6): §29 без изменений (10/13, Foundation закрыт по 3 подкритериям). Закрыт открытый вопрос отчёта Исполнителя по Блоку 2/6 (`gamification`) — `_ai/scripts/known-boundary-debt.txt` актуализирован (114→13 строк секции `gamification`, только line-shift существующих исключений, не новый техдолг), `check-module-boundaries.sh` → `OK: 0 new violations`, Блок 2/6 закрыт полностью. Текущий план (Блок 3/6, модуль `knowledge`) не двигает §29 — относится к инициативе `_ai/ROADMAP.md` «Миграция `window.*`-нарушений публичной границы модуля».

2026-07-13 (архитектор, план Блока 2/6): §29 без изменений (10/13, Foundation закрыт по 3 подкритериям). Текущий план не двигает §29 — относится к инициативе `_ai/ROADMAP.md` «Миграция `window.*`-нарушений публичной границы модуля», Блок 2/6 (`gamification`), Блок 1/6 (`ai`) закрыт исполнителем.

2026-07-13 (архитектор, документационная правка): исправлена арифметическая неточность счётчика (было ошибочно «11/13», фактически ✅ только пункты 1–7/11–13 = 10). Foundation остаётся закрыт по 3 явным подкритериям готовности (см. ниже) — счётчик пунктов таблицы не влияет на этот факт. Текущий блок архитектора не двигает §29 — по прямому указанию пользователя (2026-07-13) расширение функционала (единый справочник подрядчиков) и переезд на новые Supabase-таблицы (§38 Блоки 4/5) отложены до отдельного явного запроса; приоритет — «стабилизация и изоляция платформы/ядра от модулей» (см. `_ai/ROADMAP.md`, инициатива «Миграция `window.*`-нарушений публичной границы модуля», Блок 1/6 в работе).

2026-07-12 (сверка новой концепции пользователя с архитектурой: 7 platform-модулей классифицированы `business`/`platform` через поле `type` в манифестах без физического переноса; статус п.8 скорректирован — Блок 1 App Shell был кратко реализован и откачен по решению пользователя, подход остаётся согласованным для будущего явного запуска).
